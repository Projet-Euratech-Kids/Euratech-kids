{% block body %}
    <h1>Message de la part de {{ contact.name }}</h1><br>

    <p>{{ contact.message }}</p><br>

    <p>Numéro de téléphone : {{ contact.phone }}</p>
    <p>{{ contact.name }}</p>
{% endblock %}